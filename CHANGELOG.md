# Changelog

Dify2API 的所有重要变更均记录在此文件中。

## [v1.4.1] — 未发布（最终修复版）

> 计划中的最后版本；本版仅收口维护事项，不新增功能。

### 范围

- **流式响应错误判定**：修复空流和异常截断被误记为成功的问题。
- **管理端捐赠来源解析**：修复来源用户输入解析与保存。
- **Go 1.26.6 安全工具链**：升级发布构建工具链，消除已知可达标准库漏洞。

## [v1.4.0] — 2026-08-12

> 功能版本：用户站新增小游戏「池塘垂钓」与全站排行榜；新增 `sillytavern-main-v1` 自助下载
> 混淆模板服务并将旧 200/trimmed 标记弃用；签到默认值对齐生产实践；活跃度口径纳入游戏参与
> （与 v1.3.x 历史口径不可比）。`dify_app/` 参考模板移出仓库，改由服务端生成。数据库表/列/索引
> 由启动过程幂等创建，无需手工迁移。

### 新增

- **前端体验增强（B 方案）**：溢出标签栏在溢出时显示左右按钮与边缘渐隐提示、活动标签自动滚入
  可见区（用户站与管理员站同效，保留移动端/触摸板原生横滑、不拦截鼠标滚轮）；主体内容区移除
  固定 `max-width`、随浏览器宽度流式铺满且不产生页面级横向滚动；并改进 KPI 卡、空状态、登录页
  居中卡片、加载骨架屏与信息层级。宽表格仍在 `.table-wrap` 内局部横滑；表格超长文本（如错误详情）
  默认三行截断并可就地展开，展开态限高内部滚动，长行不再把整表顶宽。
- **小游戏「池塘垂钓」**：用户站新增「小游戏」标签页（位于积分签到与公益之间），首期上线
  池塘垂钓并预留多游戏扩展接口（按 `game_id` 维度建模）。门票制三档鱼饵（蚯蚓 5 / 商品饵 10 /
  拟饵 15 积分，全部负期望；回收率 90%，拟饵 88%），服务端签发随机种子决定渔获，客户端仅演出
  动画；渔获 = 品种 × 尺寸，倍率 0x–40x；极小概率钓上宝物（漂流瓶 / 幸运四叶草 / 神秘贝壳，
  固定 2x/3x/5x 稀有正奖励）；输局触发「永不空军」彩蛋（垃圾/小鱼苗放生，0 回收）。品种清单
  共 23 鱼种 + 8 彩蛋条目 + 3 宝物。积分下限 0 封底，输到 0 需签到回血；防刷 = 限流 + 幂等 +
  种子校验 + 0 封底，不设每日局数/亏损上限。垂钓场景采用 CSS 水面 + Canvas 波光/气泡粒子层，
  浅色主题天空绘太阳、深色主题绘月亮；鱼竿—鱼线—浮漂—鱼钩—子线—鱼为统一钓组，抛竿只抛
  钩、等待鱼靠近、收杆鱼随鱼线上移，动画与结算相位完整对应。
- **全站排行榜**：单鱼榜（单条鱼尺寸，长期累计）+ 总渔获榜（近 30 天滚动窗口，非历史累计），
  各 Top 20 并展示我的排名；用户名默认展示，提供匿名上榜开关；联动个人导出与账号删除。
- **小游戏管理（管理员站）**：新增独立「小游戏管理」标签页，提供总开关、单游戏开关、全部
  经济参数（饵价/回收率/宝物倍率）可调与一键恢复默认；v1.4.0 暂不引入专用管理员操作审计。
- **`sillytavern-main-v1` 服务**：与 `sillytavern-main-200` 同契约（system + 200 对 / 403 变量），
  由服务端按每用户随机种子生成混淆 Dify App 模板（混淆变量 key / prompt 引用 / 描述备注 /
  app 名 / 节点标题 / 节点 ID，并随机添加不定量仅混淆用 dummy 变量），生成后自校验 YAML 可解析
  与导入结构合法再提供下载。用户在「我的配置」对应服务卡片点「获取我的 App」选模型后下载 DSL
  文件，自行导入 Dify；每次重新下载会生成新种子并使旧 Dify App 立即失效（按钮旁明确提醒）。
  模板生成/下载能力经服务注册表的「可下载模板」能力通用化，不写死 v1。
- **模型配置管理（管理员站）**：管理员可对模型配置增删改查（model_key / 显示名 / provider /
  dependency 插件名 + 版本@hash / 参数设定 / 启用 / 排序）；每日 UTC 03:00 自动拉取 Dify 市场
  manifest 更新依赖版本，失败在告警中心记录一条且保留旧值，手动填写的版本标记 `manual` 不被
  自动任务覆盖。普通用户下载前选择模型。
- **公益捐赠 B' 方案（v1）**：`sillytavern-main-v1` 参与公益捐赠，但捐赠 App 与个人 App 完全
  独立（同一生成器按 `purpose` 区分）；映射快照存入 donation 行，公益路由与校验按快照进行并
  容忍 dummy 变量。捐赠 App 一经捐出不可再生：存在已激活捐赠条目则禁止重新下载；仅有未激活
  条目时重新下载需二次确认并自动使旧条目失效；无条目或全部已失效时可自由重新下载。撤捐仍仅
  管理员可操作。
- **生成记录留存**：模板生成记录（user_id / service / model_key / seed / 映射 JSON / dummy
  清单 / 生成时间 / 下载次数）纳入个人导出、账号删除匿名化与 30 天滚动清理；生成/下载计入
  request log 与限流。

### 变更

- **签到默认值对齐生产**：`checkin_min` / `checkin_max` / `credits_cap` 代码默认值由
  45 / 55 / 250 调整为 140 / 160 / 500（与生产实践一致）；既有部署的数据库设置不受影响，
  仅影响未自定义时的默认值。
- **活跃度口径纳入游戏参与**：`product_active` / DAU / WAU / Active28D / Engaged28D 的判定
  更新为 `api_successes>0 OR console_actions>0 OR game_rounds>0`，`checkin_only_active`
  相应排除有游戏活跃者；另新增 `game_rounds`（用户级每日计数）与 `game_active`（全站日聚合，
  k=5 同款抑制）指标。**此口径变化与 v1.3.x 历史口径不可比**，活跃度页与导出字段同步更新。
- **服务弃用标记与 v2 移除预告**：`sillytavern-main-200` 与 `sillytavern-main-trimmed` 标记为
  弃用（`ServiceInfo.Deprecated`），在 v1.x.x 期间继续完整支持，计划于 v2 移除；用户站与
  管理员站以 badge 标注，保存弃用服务配置时给出提示但不阻止。`sillytavern-SP·数据库-填表`
  等其余服务不受影响。

### 移除与依赖

- **`dify_app/` 移出仓库**：参考 Dify Workflow 模板（`dify_app/`）从版本库移除（`git rm --cached`
  并加入 `.gitignore`，本地文件保留）；运行时不引用该目录（无 `go:embed` 依赖），
  `sillytavern-main-v1` 改由服务端生成混淆模板。仓库内旧模板下载入口随之失效，既有用户已
  持有的 App 不受影响。
- **新增依赖**：`gopkg.in/yaml.v3`（用于 Dify App DSL 模板生成与自校验）。

### 修复

- **模板下载可导入性**：生成的 DSL 中 dummy 变量的 `options` 此前会被编码为无内容的 `!!seq`
  显式标签、`required` 键值对错位，Dify 导入报 `expected a sequence node`；现改为标准
  `options: []` 与 `required: false`，并增加结构断言防回归。
- **移除 custom-note 便签**：生成模板时删除模板自带的 custom-note 节点（其内容含中文模型提示，
  属于特征与文本泄露源），关联边一并清理。
- **混淆强化**：App 名称（原固定「Chat Workspace + 随机后缀」）、描述（原含
  “Dify2API generated workflow (v1).”）与图标（原固定 🎣）改为从多组通用词库随机选取，
  消除明显的生成器特征。
- **下载按钮文案**：弹层按钮改为「下载 DSL 文件（自用）/（捐赠）」并各附一行用途说明
  （个人模板导入自己的工作区；捐赠模板由平台分配、不可自行撤销），中英文同步。
- **抛竿频率限制**：调整为 60 秒窗口 30 次/用户（原 10 次），仍仅限制开新局，不限制结算。

### 升级提示

- 数据库表、列和索引由启动过程幂等创建（小游戏记录、模板生成记录、模型配置等），无需手工 SQL；
  升级前仍应安全备份 SQLite 数据库与 master key。静态资源与新生成资产通过 `go:embed` 进入二进制，
  拉取源码后必须重新构建并重启服务。
- **活跃度口径不可比**：本版起活跃度统计纳入游戏参与，v1.3.x 的 DAU/WAU/Active28D/Engaged28D
  等口径与本版及之后不可直接对比；如需横向比较请以本版为新基线。
- **弃用服务 v2 移除预告**：`sillytavern-main-200` 与 `sillytavern-main-trimmed` 在 v1.x.x 期间
  继续支持，计划于 v2 移除；建议新用户改用 `sillytavern-main-v1`，既有用户可择机迁移。
- 本版新增小游戏与模板下载记录属于实质性隐私政策更新（导出清单与留存说明已同步）；生产启用前
  应先发布政策更新和网站公告并至少等待 7 天。

## [v1.3.2] — 2026-08-11

> 视觉与管理维护版本：以纯 CSS 刷新用户站和管理员站，同时修复公益捐赠编辑被定价展示开关
> 错误阻断的问题；不引入数据库迁移或新增配置。

### 改进

- **清爽 SaaS 视觉主题**：统一浅色、深色与跟随系统主题的背景、层级、品牌色、卡片、表格、表单、按钮、状态徽章和 focus 样式；保持原有 DOM、交互与前端依赖不变
- **密集界面响应式体验**：tab 改为可横向滚动且隐藏滚动条，表格行操作在窄屏保持横向排列；优化手机端间距、筛选布局与宽表局部横滑，避免页面级横向溢出
- **可读性与状态反馈**：改善表格 zebra/hover、空状态、KPI、toast、dialog 与分页的视觉层次，并保持键盘 focus 可见

### 修复

- **公益捐赠编辑与定价展示解耦**：已激活捐赠不再因对应定价被禁用而无法编辑；仅在非 active → active 转换时要求定价行存在，`enabled` 继续只控制用户侧公益模型展示
- **主题边界修复**：修复浅色长页面露出深色画布、管理员英文 tab 重叠、Pico 单元格背景遮挡整行 zebra/hover，以及手机端管理员日志筛选隐式第二列造成横向溢出

### 升级提示

- 本版没有 schema、配置项或 nginx 变更，无需手工迁移；静态主题通过 `go:embed` 进入二进制，拉取源码后必须重新构建并重启服务
- 升级前仍应安全备份 SQLite 数据库与 master key；服务运行中不要只复制 WAL 模式的主 `.db` 文件，应使用 SQLite online backup 或先停服

## [v1.3.1] — 2026-08-09

> 正确性、数据完整性与运营可观测性维护版本：新增隐私保护的管理员活跃度统计，并强化公益
> 结算恢复、原子管理写入、会话期限与请求日志严格留存语义。

### 新增

- **管理员活跃度统计**：独立管理员站新增「活跃度」tab，展示 DAU、7 日 WAU、28DAU、28 日 engaged、注册总数和 7 日首次成功激活率；支持 7/28/90/400 个 UTC 日桶、distinct 活跃用户与请求量分图、控制台活跃开关、每日数据表、双语、窄屏和无障碍摘要
- **隐私保护的活动聚合**：用户级 UTC 日聚合最多保留 400 天、随个人导出且删号删除；已结束日期生成不含标识或个人筛选维度的不可逆全站匿名快照，采用 k=5（`<5`）抑制并最多保留 400 天

### 修复与强化

- **公益结算在线恢复**：短暂数据库冲突无需重启即可幂等恢复；已发送但结果不确定的调用继续保守结算，明确未消费才退款
- **管理写入原子化**：公益捐赠与定价 PATCH 统一全量验证、三态备注语义和事务提交，错误响应不再产生部分落库；共享编辑界面只发送实际变化字段
- **会话期限一致化**：浏览器 Cookie 使用创建时刻起 30 天绝对期限，数据库保持 7 天空闲期限并限制续期写频率
- **请求日志严格 30 天**：普通、公益、活跃 donation 和 orphan 日志的读取、统计、导出与物理清理均按各自时间严格滚动 30 天，绑定告警同事务删除
- **响应与校验修复**：返回完整调用方密钥的接口统一禁止缓存；管理员/Discord 登录不再吞掉 session 创建失败；防滥用最小字符数按 Unicode code point 计算，并修复中英文自动封禁参数顺序

### 升级与政策提示

- 数据库表、列和索引由启动过程幂等创建，无需手工 SQL；升级前仍应备份 SQLite 数据库与 master key
- 活跃度聚合是独立运营数据，不继承原始 request log 的 30 天期限。新增活动数据类型与最长 400 天保存期限属于实质性政策更新；生产启用采集前应先发布政策更新和网站公告并至少等待 7 天，且部署者须按当地法律审阅默认模板

## [v1.3.0] — 2026-08-07

> 功能版本：普通用户分级与协管（1–5 级、惰性判定、手动设定）、用户可见错误脱敏、
> 请求日志图表优化（本地时区小时聚合）与平台硬化（事务化写入、CSRF、会话绝对期限、
> CSV 防注入、master key 原子创建、优雅停机）。数据库无需手工迁移（`users.level`
> 列启动时幂等创建，旧数据默认自动模式）。

### 新增

- **用户分级（1–5 级）**：普通用户按公益积分自动判定等级（默认阈值 1/100/500 对应 2/3/4 级，5 级仅管理员手动设定）；等级在读取时惰性计算，门槛或积分调整即时生效。管理员可自定义各等级名称（留空回退数字等级）与门槛（校验 `0 ≤ t2 < t3 < t4`），配置走专用原子接口，杜绝部分写入
- **等级特权**：2 级及以上显示用户站顶部横幅（管理员配置的纯文本，不可关闭）；3 级签到无视积分上限（`credits_cap=0` 的全局关闭仍优先）；4 级可在用户站审批捐赠申请（单条与批量，约束与管理员一致）；5 级可管理公益资源与定价表、查看全站请求日志与统计（**无导出**，错误详情按用户视角脱敏）
- **管理员分级管理**：新增「用户分级」tab（等级名称/门槛/横幅）；用户管理列表显示等级（自动/手动）并支持手动设定或恢复自动判定
- **用户站等级徽标与协管入口**：顶部显示当前等级（自定义名或数字回退）；4/5 级新增「公益协管」tab（4 级仅审批面板，5 级再加资源与定价管理），5 级新增「全站日志」tab（与管理员同款小时统计图）
- **日志图表按本地时区聚合**：统计改为按小时聚合、在浏览器本地时区归并为天（DST 切换安全）；图表跟随列表筛选，查询时表格与图表同时刷新；无筛选时展示 30 天留存范围内的全部历史
- **按服务统计接口 501**：`by_service=1` 请求返回 501 Not Implemented（响应保留兼容的空 `by_service` 字段），旧 `days` 参数接收但忽略
- **捐赠筛选 2×2 布局**：管理员公益资源筛选区改为网格布局（宽屏两列、窄屏一列），即时客户端过滤行为不变

### 修复

- **管理员用户搜索兼容 Discord ID**：用户管理页的统一搜索框可按 Discord 用户 ID 精确筛选，行为与数字站内 ID、用户名筛选保持一致
- **用户可见错误脱敏**：所有普通用户输出（阻塞响应、流式错误帧、App 探测、用户日志、用户导出、5 级全站日志）统一剥离 URL、域名、IP 与疑似密钥，只保留错误类别与友好提示；管理员日志、导出、告警与服务器日志保留完整原文便于排查
- **公益结算保守化**：已发送但结果不确定的公益调用按成功保守结算，不再按普通失败退款，避免与审批/删除并发造成账务不一致
- **审批与删除事务化**：捐赠申请审批、驳回、批量操作与账号删除改为事务内条件抢占，并发 approve/reject 只有一个终态
- **管理员写入原子化**：设置、防滥用、公告、定价与各类批量写入改为单事务，不再出现部分写入或残留旧值
- **CSRF 防护**：基于 Cookie 会话的状态变更请求新增同站 Origin 校验，跨站表单/脚本无法再伪造管理或用户操作
- **会话绝对期限**：会话新增 30 天绝对上限（滑动续期不超过创建时刻 + 30 天），限制窃取令牌的长期重放
- **错误响应统一 envelope**：各 Web 接口错误统一 `{"error":{"code","type","message"}}` 结构并补齐安全响应头（nosniff/DENY）
- **CSV 导出防公式注入**：管理员日志 CSV 导出对单元格首字符转义，防止电子表格软件把导出内容当公式执行
- **master key 原子创建**：首次启动生成 key 改为同目录完整写入后 no-replace 原子发布，校验文件类型/权限/长度，兼容并发启动；旧 key 永不覆盖
- **优雅停机**：新增可配置停机期限；HTTP 排空、后台清理、mailer、登录/IP 限流与用户 Debug 会话均有停止/等待路径，超时强制关闭后再关数据库

### 升级提示

- 数据库表和列由启动过程幂等创建：`users.level` 列（NULL=自动，1–5=手动）自动添加，无需手工 SQL，旧数据默认按自动模式计算等级
- 降级到旧版本时，旧二进制忽略新列，仅等级功能降级，不损坏数据（由迁移兼容测试保证）

## [v1.2.1] — 2026-08-05

> 维护版本：签到可突破积分上限、公益捐赠列表筛选与资源可用展示、自助捐赠服务开关、
> 告警中心每类双开关（记录 + 邮件）、用户数字 ID 展示与筛选，以及多项公益结算与
> 运维修复。数据库无需手工迁移（新表/新列均幂等自动完成）。

### 新增

- **对应源码链接（AGPL-3.0 §13）**：新增 `SOURCE_URL` 配置（默认上游仓库），服务协议页中英文均展示获取对应源代码的仓库链接；部署修改版时应改为自己公开发布的地址
- **签到可突破积分上限**：单次签到奖励不再被 `credits_cap` 硬截断（如 499 分签到获得 140–160 随机奖励可超过 500 上限），但保持“发起签到时积分已达上限则签到失败”的既有设定；状态接口新增 `capped` 字段，达到上限时签到按钮直接禁用并提示
- **公益（已批准）捐赠列表筛选**：管理员公益资源页新增筛选行（状态/服务/关键字，关键字匹配模型、来源与备注），并支持按用户（用户名、Discord ID 或数字 ID）筛选
- **自助捐赠服务开关**：防滥用表格原先空置的「操作」列改为「自助捐赠可选」开关，按服务控制普通用户自助捐赠申请时可选择的服务；`/api/services?donation=1` 返回可选服务，提交接口同步拒止（纵深防御），管理员创建捐赠不受限
- **告警中心分类开关**：新增「邮件与告警通知设置」表格，六类事件（用户自动封禁、捐赠自动未激活、管理员登录锁定、公益定价缺失、Debug 滥用、阻塞 200 但失败）分别控制是否在告警中心记录与是否发送邮件，默认全部开启；邮件事件现在也会写入告警中心（默认开）；同时修复 mailer 冷却窗口此前取默认值而非管理员设置 `mailer_cool_minutes` 的问题
- **用户数字 ID 展示与筛选**：管理员用户列表新增 ID 列；用户搜索、日志筛选、捐赠筛选均支持按数字 ID 精确筛选（筛选框下拉项同步显示 `[id]`）
- **公益定价资源可用列**：用户端定价列表新增「可用资源」列，按是否存在 active 且未到期且有剩余次数的捐赠显示可用/暂不可用（此前仅按“存在任意状态捐赠”过滤，资源耗尽后仍显示）

### 修复

- **移除从未使用的告警类型**：`donation_exhausted_race` 常量与界面文案从未产生任何记录，一并移除
- **公益捐赠到期状态未落库**：到期扫描此前虽有数据库方法和单元测试，却未接入运行时；现在启动时及每分钟将 active/inactive 的逾期条目标记为 expired，管理员列表也会即时补扫
- **公益流式中断按成功调用结算**：Dify 已返回 HTTP 200 并开始 SSE 后，即计入 B 类 RPM、捐赠成功次数和积分扣减；后续传输中断不再增加捐赠失败次数或触发自动停用
- **公益失败日志补齐来源**：选定捐赠条目后的流式/阻塞/图片上传/凭据及出站失败日志现在都保留 `donation_id`，管理员端可显示捐赠来源
- **管理员 SPA 首屏加载竞态**：common.js 的异步初始化可能在 user.js/admin.js 执行前完成，导致 `renderAdminDashboard is not defined`；现在等待 DOMContentLoaded 后再启动路由
- **公益阻塞调用超时提示本地化**：公益路径的 `upstream_timeout` 错误消息此前硬编码中文；现在普通与公益阻塞路径均按调用者账号的语言设置返回中/英文本
- **自站引导挂起状态不再跨渲染残留**：命中自站提示后若直接切换页面再返回，新建配置不会再被误路由为更新上一条配置
- **UNIQUE 冲突判断改用驱动错误码**：配置创建/更新的同名冲突（409）此前依赖匹配驱动错误文案，现改用 SQLite 约束错误码（`db.IsUniqueViolation`），避免驱动文案变化导致误报 500
- **自站地址误填引导对齐创建路径**：把本站控制台地址误填为 Dify App 端点时，创建配置表单不再闪现警告后直接刷新列表，而是保留表单输入、在地址输入框旁持续显示醒目警告；修正地址后再次保存会就地更新刚创建的配置（避免同名冲突），也可显式确认继续。自助捐赠表单同步保留输入（此前提示出现前已清空表单，与“保留表单”注释意图不符）

## [v1.2.0] — 2026-07-31

> 安全加固版本：出站防护收窄（精确 origin + 自站永久拒绝）、用户调试资源边界、
> App 探测加固、公益持久化结算与多项运维修复；同时带来用户引导提示等体验改进。

### 新增

- **公益持久化预留账本**：新增 `charity_reservations`，以
  `reserved → dispatched → committed/released` 状态机记录每次公益调用；价格、奖励和捐赠者均在预留时快照，启动时自动恢复中断记录
- **管理员日志图表升级**：内嵌并按需加载经完整性校验的 Chart.js 4.5.1，展示最近七天成功/错误堆叠图和按服务统计；支持主题、双语、响应式及无障碍文本摘要
- **出站与代理安全配置**：新增 `TRUSTED_PROXY_CIDRS`、`DIFY_EGRESS_ALLOWLIST`、
  `REMOTE_CONTENT_ORIGIN_ALLOWLIST`、`DIFY_MAX_RESPONSE_MB`、`DIFY_PROBE_IN_FLIGHT` 和 `MAX_WEB_REQUEST_BODY_KB`
- **第三方许可清单**：新增 `THIRD_PARTY_NOTICES.md`，记录 Chart.js、`@kurkle/color` 与 Pico CSS 的 MIT notice
- **用户自助 Debug 资源与秘密边界**：请求/inputs/响应捕获前截断（64 KiB/64 KiB/256 KiB）并带截断标记；事件 Header 只回传安全白名单（不含认证/Cookie）；通道容量与每 session 累计字节设硬上限，溢出丢弃最旧事件并推送 `dropped` 通知；未连接 SSE 10 秒自动关闭、空闲 5 分钟、最长生命周期 1 小时
- **App 兼容性探测加固**：单次总 deadline 15 秒（覆盖排队与上游）、每用户滑动窗口限流（默认 5 次/分钟，管理端可调 `probe_limit_per_user`）、无变化更新自动跳过探测；限流/超时不误报不兼容
- **告警中心联动**：`blocking_failed_200` 告警绑定关联请求日志并带出所属用户，「查看关联请求」一键跳转日志页并按用户过滤、高亮目标行（随日志 30 天留存同事务清理）
- **用户引导（安全审计建议项落地）**：注册服务/编辑配置/自助捐赠时，若把本站自身地址（`SITE_BASE_URL`/`ADMIN_HOST`，含 `/v1` 变体）误填为 Dify App API 端点，响应返回友好提示且仍正常保存；只比对公开 hostname，不影响保存也不产生出站侧信道

### 变更

- **Dify 出站默认拒绝私网**：阻断 loopback、私网、link-local、metadata、DNS rebinding、跨 origin 重定向和环境代理；私有 Dify 必须由部署者显式允许
- **出站 allowlist 收窄为精确 origin**：`DIFY_EGRESS_ALLOWLIST` 只接受 `http(s)://host[:port]`（拒绝 CIDR、裸 IP、通配符与路径）；网关自身的 `SITE_BASE_URL`/`ADMIN_HOST` 以及 loopback 监听地址即使出现在 allowlist 中也永久拒绝（拨号阶段返回与真实不可区分的 connection refused）
- **远程内容默认禁用**：`website-summary` URL 与远程图片只有精确 origin 位于允许列表时才会交给 Dify 抓取；参考 Website Summary DSL 同步缩短网络超时和重试
- **请求生命周期与资源上限**：所有 Dify 请求绑定下游 context；客户端断开会取消上游并使用独立短 context 尝试 Stop；限制解压后 JSON、累计 SSE 和 App 探测并发
- **公益结算事务化**：捐赠次数、消费者积分和 reservation 在同一 SQLite 事务内预留；明确前置/上游失败原子退款，已 dispatched 的不确定结果保守结算；捐赠者贡献和奖励幂等提交
- **公益 RPM 原子占位**：契约、远程 URL 和 debug dry-run 校验提前到占位前；失败可释放 lease，避免并发穿透或无效请求消耗额度
- **日志严格逐条留存 30 天**：普通、公益、活跃 donation 和 orphan 日志均按自身 `started_at` 清理；绑定告警同事务删除，未绑定告警保留人工处理
- **OAuth state 无状态化**：改为 HMAC 认证、时限约束且绑定 cookie 的 token，不再维护可被匿名请求放大的内存 map
- **SSE 默认内存降低**：`SSE_BUFFER_MB` 默认值由 10 MiB 降至 1 MiB；单行上限 10 MiB，单次累计响应默认 32 MiB
- **Debug 开关语义统一**：「发送到 Dify」开关现在直接表示是否真实转发（默认关闭 = 演习模式），与提示文案一致，避免普通用户误解

### 修复

- 修复管理员日志筛选区查询/导出按钮在桌面及移动端重叠
- 修复公益模型早期失败日志将 service 记为“公益”而非实际服务的问题
- 修复并发公益调用可能超出捐赠次数、产生负积分、重复奖励或错误 `credits_consumed` 的账务竞争
- 修复签到更新可能以旧快照覆盖并发积分扣减的问题
- 修复用户 Debug session 的状态读写、channel send/close、替换与延迟清理竞争；重连不再被旧 timer 关闭
- 修复 anti-abuse 配置刷新与请求/admin 读取之间的 map 竞争
- 修复管理员日志 CSV/JSON 导出被分页钳制静默截断为 100 条的问题（导出改为无上限查询，列表分页不受影响）
- 修复 egress self-guard 在 `LISTEN_ADDR=localhost` 时未登记 loopback 地址、误将网关自身 loopback origin 加入 allowlist 即可绕过自拨阻断的缺口
- 修复管理员登录用户名未限长导致的登录节流内存膨胀风险（throttle key 截断至 128 字节）
- 修复管理端下调捐赠 `total_count` 可能使 `remaining_count` 为负的问题（钳制到 0）
- 修复告警中心「查看关联请求」不切换到日志页导致点击无反应的问题
- 修复页脚「隐私政策 · 服务协议」链接文案不跟随语言切换的问题

### 安全加固

- 仅信任 `TRUSTED_PROXY_CIDRS` 中 TCP peer 提供的转发头，从右向左解析代理链；未知 Host 在重定向前返回 421，HTTPS 重定向使用固定配置 origin
- 状态变更类 Web API 增加统一 256 KiB 请求体上限；chat 继续使用独立 10 MiB 上限
- Dify 响应体、SSE 背压发送和 connectivity probe 均增加硬上限或并发门禁
- nginx 示例改为覆盖 XFF，并同步 Host、body 上限和流式代理安全设置
- 自拨阻断无侧信道：指向网关自身的拨号在 dial 阶段返回伪造 ECONNREFUSED 并附加 150–350ms 随机抖动，与探测任意死公网 IP 不可区分；保存阶段不区分自站与非自站

### 升级提示

- 数据库表和索引由启动过程幂等创建，无需手工 SQL；升级前仍必须备份 SQLite 数据库及 master key
- 使用 Docker、远端或多级反代时，必须把实际代理来源 CIDR 加入 `TRUSTED_PROXY_CIDRS`；不要使用 `0.0.0.0/0` 或 `::/0`
- 现有 Dify Base URL 若解析到私网，升级后会返回 `upstream_blocked`，须先配置最小范围的 `DIFY_EGRESS_ALLOWLIST`
- 依赖任意 website-summary URL 或远程图片的部署，须按业务需要配置精确的 `REMOTE_CONTENT_ORIGIN_ALLOWLIST`；默认空列表会拒绝这些请求
- 静态资源要求 CDN 遵守 origin revalidation；`index.html` 最终保持 `Cache-Control: no-cache`，并未改为 `no-store`

## [v1.1.0] — 2026-07-29

> 功能增强与打磨版本。公告系统 Markdown 支持、全站 i18n 错误消息、
> 法律页面英文版、日志导出与图表、移动端响应式适配。

### 新增

- **公告系统增强**：`bulletins` 新增 `content_type` 列（html/markdown），集成 goldmark 服务端 Markdown 渲染，管理端加语言/格式选择器
- **邮件系统增强**：mailer 发送成功日志；userDebugHub 滥用检测（5 次/10 分钟阈值），新增 `EventDebugAbuse` 告警事件
- **法律页面英文版**：新建 `privacy/terms/maintenance.en.html`；服务端按 `?lang=en|zh` 或已登录用户语言偏好选择版本，未登录且未指定参数时默认中文
- **用户端增强**：`/api/me` 新增 `donation_credit` 统计；驳回的捐赠申请支持复制信息重新提交；管理端新增申请历史筛选列表（按状态/用户）
- **API 错误消息 i18n**：新建 `handler/i18n.go`（canonical 语言检测 + `t` 辅助函数），主要网关错误消息实现中英双语
- **管理员日志导出与图表**：`/api/admin/logs/export`（CSV/JSON 格式）、`/api/admin/logs/stats`（每日统计 + 按服务统计）、Canvas 柱状图
- **移动端响应式适配**：768px/480px 两级断点，dialog 全屏化，nav 折叠，table 次列隐藏，44px 触控目标最小尺寸

### 修复

- 修复管理员请求日志 Canvas 图表颜色与高度
- 修复管理端申请历史默认分页大小与下拉选项不一致
- 曾尝试将 `index.html` 改为 `no-store`，发布前已回滚；v1.1.0 最终行为保持 `no-cache`

## [v1.0.1] — 2026-07-29

> 缺陷修复与小改进。

### 修复

- **P0**：管理员编辑捐赠弹窗的备注字段改 `note` → `review_note`
- `ApproveApplication` 创建捐赠时填入 `source_discord_id` 与 `source_username`

### 新增

- 三处 Discord ID 可点击复制（用户管理、待审核、捐赠列表）
- 手动主题切换（亮色/暗色/跟随系统），`localStorage` 持久化

## [v1.0.0] — 2026-07-27

> 首个正式稳定版（GA）。经过 8 个预发布版本的迭代，Dify2API 现已具备完整的
> 多用户 API 网关能力、内嵌管理台、公益积分体系和防滥用保护。

### 核心能力

- **OpenAI 兼容 API 网关**：`/v1/chat/completions`（流式/非流式）、`/v1/models`
- **多用户隔离**：Discord OAuth 登录（guild + role 门槛），每用户自行绑定 Dify App 凭据
- **双站点架构**：主站（用户 Discord 登录）+ 管理站（admin 子域，密码登录），严格 host 隔离
- **凭据安全**：AES-GCM 加密存储 API 密钥，SHA-256 索引防重复检测

### 支持的服务

| 服务 | 说明 |
|------|------|
| `general` | 通用单轮问答（仅 user_0） |
| `custom` | 自定义单轮问答（user_0 + 可选 system_prompt） |
| `website-summary` | 网页总结（request_url + 可选 instruction） |
| `image-processing` | 图片理解（system_prompt 可选 + user_request + 图片） |
| `sillytavern-main-trimmed` | SillyTavern 主对话（1–22 条宽松角色槽位布局） |
| `sillytavern-main-200` | SillyTavern 大容量对话（1–403 条，200 对 user/assistant） |
| `sillytavern-SP·数据库-填表` | SillyTavern 数据库填表（system + 可选 user + 严格交替 assistant/user + 预填充） |

### 管理台功能

- 用户管理（密钥重置、数据导出、封禁/解封）
- 捐赠审批（三态：pending/active/inactive）+ 批量操作
- 公益资源库（per-donation RPM 限制、加权路由）
- 按模型定价表（独立于全局设置，含积分回报）
- 公告栏（多语言、维护模式切换）
- 防滥用配置（三档开关：off/charity/all，按服务定制惩罚）
- 请求日志（含防滥用触发详情、积分消耗记录）
- 告警中心（邮件聚合：连续失败、定价缺失、用户封禁）
- 管理员单用户数据导出（合规）

### 用户端功能

- API 密钥管理（创建/删除/调用统计）
- Dify App 配置绑定（连通性测试）
- 公益捐赠提交（Key 重复检测、撤回警告）
- 积分签到（随机奖励、日上限、时区偏移）
- 请求日志查看（含防滥用惩罚友好展示）
- 自助 Debug（SSE 流式推送至浏览器，dry-run 模式）
- 自助数据导出/账号删除（合规）

### 安全与防护

- 三类 RPM 限流（A/B/C：传输完成/请求成功/请求接收）
- Web IP 限流 + 无效密钥节流
- 登录爆破防护（滑动窗口 + 恒定时延）
- 防滥用机制（角色校验 + 内容长度门禁，扣分/封禁惩罚可配置）
- 并发信号量（`MAX_CHAT_IN_FLIGHT`，默认 32）
- 客户端断开时自动停止上游 Workflow
- 维护模式（API 返回 503，管理台仍可访问）
- Force HTTPS（301 重定向）

### 性能基线

- Chat completions 网关自身处理：~1,900 RPS（非流式）、~1,475 RPS（流式）
- `/v1/models`：~37,000 RPS
- 测试环境：Go in-process httptest（消除网络延迟），stub Dify 0ms 响应
- 内存模型：每流式连接预分配 `SSE_BUFFER_MB` 字节（默认 10MB），`MAX_CHAT_IN_FLIGHT=32` 安全适配 1GB VPS

### 技术栈

- **后端**：Go ≥1.25 + SQLite（modernc.org/sqlite，免 CGO）
- **前端**：原生 JS + Pico.css，go:embed 零构建链
- **插件**：Pi Coding Agent 子智能体（TypeScript + esbuild）
- **部署**：单二进制，无外部服务依赖
